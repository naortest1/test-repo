name: log and process issue comments
on: [push]


jobs:
  issue_comment:
    name: log issue comment
    runs-on: self-hosted
    steps:
      - name: Test
        uses: dummy-guy-organization/hello-world-typescript-action@v2
        with:
          who-to-greet: Naor
      - name: Print dirs
        uses: actions/github-script@v3
        with:
          script: |
            ls -al
          result-encoding: string
